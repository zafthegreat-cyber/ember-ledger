const fixture = (id, scenario, expected = {}) => Object.freeze({
  id,
  scenario,
  synthetic: true,
  sourceReference: `source.${id}.test`,
  expected: Object.freeze(expected),
});

/** Reserved-only QA descriptors. They are never loaded as application data. */
export const PHASE2CB_QA_FIXTURES = Object.freeze([
  fixture("one-received-sealed-product", "STANDARD", { eligible: true }),
  fixture("multi-quantity-sealed-product", "MULTI_QUANTITY", { eligible: true }),
  fixture("three-unit-penny-remainder", "PENNY_REMAINDER", { unitCosts: [334, 333, 333] }),
  fixture("partial-receiving-four", "PARTIAL_RECEIVE_4", { eligible: true }),
  fixture("partial-receiving-six", "PARTIAL_RECEIVE_6", { eligible: true }),
  fixture("multi-line-purchase", "MULTI_LINE", { eligible: true }),
  fixture("target-purchase", "RETAILER_TARGET", { eligible: true }),
  fixture("walmart-purchase", "RETAILER_WALMART", { eligible: true }),
  fixture("matched-product", "MATCHED", { eligible: true }),
  fixture("ambiguous-product", "AMBIGUOUS", { eligible: false }),
  fixture("unresolved-product", "UNRESOLVED", { eligible: false }),
  fixture("damaged-item-hold", "DAMAGED_HOLD", { eligible: false }),
  fixture("damaged-item-reviewed", "DAMAGED_ADD", { eligible: true }),
  fixture("wrong-item", "WRONG_ITEM", { eligible: false }),
  fixture("substituted-item", "SUBSTITUTED_ITEM", { eligible: false }),
  fixture("unexpected-extra", "UNEXPECTED_EXTRA", { eligible: false }),
  fixture("cancelled-unreceived", "CANCELLED", { candidateCount: 0 }),
  fixture("partial-cancellation", "PARTIAL_CANCELLATION", { eligible: false }),
  fixture("refund-without-return", "REFUND_ONLY", { inventoryMutation: false }),
  fixture("return-after-creation", "RETURN_REVERSAL", { reversalRequired: true }),
  fixture("partial-return", "PARTIAL_REVERSAL", { reversalRequired: true }),
  fixture("replacement", "REPLACEMENT", { automaticCreation: false }),
  fixture("duplicate-confirmation", "DUPLICATE_CONFIRM", { canonicalResults: 1 }),
  fixture("repeated-candidate", "REPEATED_CANDIDATE", { canonicalResults: 1 }),
  fixture("interrupted-write", "WRITE_FAIL_BEFORE", { retrySafe: true }),
  fixture("interrupted-repair", "AMBIGUOUS_WRITE", { retrySafe: true }),
  fixture("two-tab-collision", "CONCURRENT_CONFIRM", { canonicalResults: 1 }),
  fixture("stale-version", "STALE_VERSION", { blocked: true }),
  fixture("existing-inventory-product", "EXISTING_PRODUCT", { newLot: true }),
  fixture("new-lot-existing-product", "NEW_LOT", { newLot: true }),
  fixture("lot-quantity-greater-than-one", "LOT_MULTI_QUANTITY", { eligible: true }),
  fixture("cost-remainder", "COST_REMAINDER", { exact: true }),
  fixture("duplicate-receiving-event", "DUPLICATE_RECEIVING", { canonicalResults: 1 }),
  fixture("same-line-multiple-receives", "MULTIPLE_RECEIVES", { separateLots: 2 }),
  fixture("sold-quantity-before-reversal", "SOLD_BEFORE_REVERSAL", { blocked: true }),
  fixture("insufficient-reversal-quantity", "INSUFFICIENT_REVERSAL", { blocked: true }),
  fixture("owner-condition-correction", "CONDITION_REVIEW", { eligible: true }),
  fixture("owner-product-resolution", "PRODUCT_RESOLUTION", { eligible: true }),
  fixture("secret-bearing-evidence", "SECRET_REJECTION", { rejected: true }),
]);

export const PHASE2CB_FIXTURE_COUNT = PHASE2CB_QA_FIXTURES.length;
